# GitHub release text (draft — not published)

Title: **v0.3.0 — wrap an MCP server, decide on arguments, keep denials from coming back**

Body: use `docs/release/v0.3.0-notes.md` verbatim, followed by:

> Feedback wanted on three things: (1) whether `atp mcp init` proposes sane
> capabilities for your server, (2) whether shadow mode is enough to trust
> turning enforcement on, (3) what you would need before running `atp serve`
> for real. Open an issue; the compatibility matrix says what has and has
> not been tested.

Repository description (≤ 350 chars):
"Authorization boundary + security regression tests for AI agent tool calls. Wrap an MCP server; decide on arguments against delegated authority; bind decisions to single-use grants; replay, explain, and pin denials as CI tests. Experimental."

Topics (accurate only): ai-agents, mcp, model-context-protocol, agent-security, ai-security, authorization, llm-security, ai-evals, security-testing, red-team, policy-engine.

Social preview: `docs/images/demo-wrap.svg` rendered to PNG (real transcript).
